[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "my_rust_agent"
version = "0.1.0"
requires-python = ">=3.8"
description = "PCAM Hopfield Network precision engine for Python"

[tool.maturin]
features = ["python"]
